{
  "name": "wbs-movie-diary",
  "version": "2.0.0",
  "description": "Refactoring existing Movie Diary to the modular design using Parcel",
  "source": [
    "src/pages/index.html"
  ],
  "scripts": {
    "dev": "parcel",
    "build": "parcel build src/pages/index.html --dist-dir build --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.16.3"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
